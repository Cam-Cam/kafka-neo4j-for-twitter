package worker

import org.apache.avro.generic.GenericRecord
import org.scalatest.FunSuite
import twitter.Tweet

import scala.io.Source
import org.apache.avro.generic.GenericRecord

class WorkerTest extends FunSuite {

//  test("Collection of one tweet"){
//
//
//
//
////      .fromURL(getClass.getResource("/randomAvroRecord"),classOf[AvroKeyInputFormat[GenericRecord]).asInstanceOf[GenericRecord]
////    val conf = sc.hadoopConfiguration
////    sc.newAPIHadoopFile(path, classOf[AvroKeyInputFormat[GenericRecord]], classOf[AvroKey[GenericRecord]], classOf[NullWritable], conf).map(_._1.datum().asInstanceOf[MyRecord])
//
//
//Worker.collectTweetInformation(record)
//
//    println(Tweet)
//  }

}
